export const en = {
  'app.title': 'Knowledge Scope Manager',
  'scopes.don’t.title': 'Create a scope',
  'scopes.create.hint': 'Choose the sources this scope can see.',
} as const;

export type I18nKey = keyof typeof en;
