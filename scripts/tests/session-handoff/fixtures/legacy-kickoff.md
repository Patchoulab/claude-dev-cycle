Continue the project-planning rollout from where we left off. Finish wiring the
handoff_lint edge cases into the test suite, make sure the CLI checks pass, and
update the README once that's done.

Also take a look at whether the review harness gate still spawns a reviewer
correctly after the last round of hardening -- there was some flakiness
reported in the last session.

Ping the team once the branch is green.
