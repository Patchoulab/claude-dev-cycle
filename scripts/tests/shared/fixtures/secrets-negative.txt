const apiKey = process.env.API_KEY;
UNIFI_KEY=  # value lives in .env, never committed
token = get_token_from_infisical("path/to/key")
# see docs/secrets.md for the granting ritual
password_field_label = "Password"
DATABASE_URL=postgres://${DB_USER}:${DB_PASS}@db:5432/app
const skKey = "sk-not-a-real-secret-just-a-slug-value";
# rotate anything that starts with the github_pat_ prefix (docs reference)
