---
name: capture-workflow
description: Capture a completed session or workflow into a reusable command, skill, playbook, or cron job
argument-hint: "[what to capture, optional]"
---
Invoke the dev-cycle capture-workflow skill with arguments: $ARGUMENTS
Read ${CLAUDE_PLUGIN_ROOT}/skills/capture-workflow/SKILL.md and run its
five-stage pipeline (extract → classify → author → validate → package).
