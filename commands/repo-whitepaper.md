---
name: whitepaper
description: Build an interactive whitepaper site from this repo's markdown
---
Use the dev-cycle:repo-whitepaper skill on $ARGUMENTS (default: the current
repo). Follow all its gates in order; do not skip the sensitivity sweep or
the runtime verification sweep.
